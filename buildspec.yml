version: 0.2 
phases:
  install:
    runtime-versions:
      java: corretto11
  pre_build:
    commands:
      - cd src/main/resources/static
      - cd $CODEBUILD_SRC_DIR
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
artifacts:
  files:
    #- target/devops-2.0.0-SNAPSHOT.war
